@using JobBoard.Models;

@if (Model.Count == 0)
{
    <p>There are no jobs in this list.</p>
}
<h1>Job Board</h1>
<ul>
    @foreach (JobOpening JobOpening in Model)
    {
        <li>@JobOpening.Title, @JobOpening.Description</li>
    }
</ul>
<a href="/jobs/new"> Add a new job posting.</a>